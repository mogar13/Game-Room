# Normalize all text to LF in the repo and in the working tree.
# Static files are served as-is, so keep line endings stable across machines.
* text=auto eol=lf

*.png binary
*.mp3 binary
*.ogg binary
